# 验收记录与剩余验证

日期：2026-09-09。范围是个人使用的 Windows 首版。M0–M3 的功能已实现；M4 已完成本机打包与隔离运行验证，尚未在另一台完全未配置的 Windows 机器上复验。

## 自动化验证

| 检查 | 本机结果 |
| --- | --- |
| `cargo test --workspace` | 61 项通过；另有 1 个子进程辅助入口仅由父测试启动，不单独运行 |
| `cargo clippy --workspace --all-targets -- -D warnings` | 通过 |
| `cargo fmt --all -- --check` | 通过 |
| Python worker unittest | 8 项通过，不需加载模型 |
| 前端 Vitest | 4 项通过 |
| TypeScript / Vite production build | 通过 |
| Tauri Windows NSIS build | 已生成 x64 安装包 |

合计 73 项自动化测试通过（Rust 61、Python 8、前端 4）。

测试覆盖字幕状态区分、四种导出、VTT 字符转义、中文索引、版本事务、旧引用、批量导入失败回滚、任务完成与文字原子提交、重复提交幂等、资料库占用、重启恢复、取消重试及 Windows 进程树终止。缓存类别根 junction 不能删除原始文件。stdin 不被工具读取时仍受超时控制。新增旧音轨路径回归测试：返回播放器前规范化父级路径，音轨已删除时保留文字稿并隐藏失效播放器。

## 真实本机与桌面操作

- 原始链接可读取 69 P 信息，默认只勾选 P1。未处理全部课程。
- 《课程目标》356.608 秒真实音轨通过 Rust 下载/转换/Whisper GPU/SQLite 完整路径，保存 158 个片段；没有下载视频画面。
- 独立 small/CUDA、small/CPU 小样本与强制终止后恢复都已实际运行。具体时间与峰值见 [benchmarks](../benchmarks/README.md)。
- 实际 Tauri WebView2 界面通过字幕导入、片段选择、手写笔记、校对新版本、旧引用跳转、四种导出、中文搜索、未保存笔记切换保护。
- 设置资源页、共享模型只读状态、B 站分 P 范围已检查。导出的 Markdown 引用存在对应锚点。界面 console 无错误。
- NSIS 按当前用户成功安装到 D 盘。安装版从 `http://tauri.localhost/` 加载内置页面；停止 Vite、移除 Node/Rust/Python 的 PATH，并将 Python/FFmpeg/模型设置清空后，仍能完成字幕导入和带引用笔记保存。
- 最终安装版读取真实《课程目标》音轨的 356.608 秒时长，时间戳播放和后退 5 秒均正常；修复了旧缓存路径含父级目录时的回听失败。保存并在重装后读回“课程目标：学习重心”笔记，导出的 Markdown 包含 158 个片段和 5 条已核对的原视频出处链接。

## 模拟和 mock 的范围

CPU/4 GB 策略有本机依据；8 GB、24 GB 和不兼容驱动仅用构造数据验证推荐策略，不记为硬件实测。

知识接口使用真实回环 HTTP 配合模拟响应，验证选中文字、有效/无效引用、材料不足、401、429、超时、无效 JSON 及超过 8 MiB 的 chunked 响应。安装版也用明确标记为“模拟接口验证”的本机服务测试 UI 到 Rust 到 HTTP 的引用笔记流程。这些不是云端模型质量测试。

## 仍需验证

1. 未提供真实云端 API 配置与密钥：实际服务商兼容性、摘要/问答质量、计费和额度未测试。
2. 未建立人工参考转写：中文 CER 和术语准确率没有结果。现有转写能见到“经济网络”等疑似误识别，需要回听校对。
3. 更长课程、中英混合、背景噪声、多模型对照仍需增加样本；已有测试不能代表所有材料。
4. 未在其他显卡或第二台干净 Windows 上安装验证。隔离 PATH 与空依赖设置测试不能替代完整干净系统测试。
5. 安装包未使用商业代码签名证书。额外 CPU 环境安装脚本提供独立安装路径，未覆盖所有 Python、网络镜像和驱动组合。

在这些边界内，首版可用于本机的课程导入、文字校对、可追溯笔记与导出。后续跨课程语义检索、更多 ASR/GPU 后端和更多网站不作为首版已交付能力。
